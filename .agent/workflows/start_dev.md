---
description: Start the development environment
---
1. Install dependencies if not already installed (one-time setup)
```bash
npm install
```
2. Start the development server (Frontend only)
```bash
npm run dev
```
3. Start the application with Tauri (Frontend + Backend)
*Requires Rust installed*
```bash
npm run tauri dev
```
